schema_version: 1

name: &name "devspaces-terraform"
version: &version "1.0"
description: "Developer image for Red Hat OpenShift Dev Spaces with terraform, ansible and bind-utils"

modules:
  repositories:
  - path: "../modules"
  install:
  - name: terraform
  - name: ansible
  - name: bind-utils
